# plaintext secrets (never commit)
*.dec
*.decrypted
*.plain
**/.env
**/.env.*
**/secrets/*.env
**/secrets/*.json
**/secrets/*.yaml
**/secrets/*.yml

# allow encrypted secrets (we’ll use .sops.* or .enc.* names)
!**/secrets/*.sops.*
!**/secrets/*.enc.*
