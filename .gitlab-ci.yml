deploy-traefik:
  stage: deploy
  tags: ["traefik"]
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - hosts/traefik/**/*
  script:
    - whoami
    - pwd
    - ls -la
    - ls -la hosts/traefik/secrets
    - bash hosts/traefik/scripts/deploy.sh
